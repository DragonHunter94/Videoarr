# Default server configuration for nginx proxy
server {
    listen 80;
    server_name _;

    # Redirect to main application
    location / {
        return 301 http://$host:3000$request_uri;
    }
}